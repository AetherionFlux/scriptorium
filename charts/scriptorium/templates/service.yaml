apiVersion: v1
kind: Service
metadata:
  name: {{ include "scriptorium.fullname" . }}
  labels:
    {{- include "scriptorium.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "scriptorium.selectorLabels" . | nindent 4 }}
